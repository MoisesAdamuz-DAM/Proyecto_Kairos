<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="Kairos.Paginas.Persona.ModificarPersona"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">
    <StackLayout Padding="10" Spacing="10">
        <Entry x:Name="txtNombre" Text="{Binding Nombre}" />
        <Entry x:Name="txtPais" Text="{Binding Pais}" />
        <Entry x:Name="txtUbicacion" Text="{Binding Ubicacion}" />
        <Entry x:Name="txtNecesidad" Text="{Binding Necesidad}" />
        <Entry x:Name="txtHistorial" Text="{Binding Historial}" />
        <Button x:Name="btnDatos" Text="Modificar" />
    </StackLayout>
</ContentPage>